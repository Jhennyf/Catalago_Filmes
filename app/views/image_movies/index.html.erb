<%= turbo_frame_tag "image_movies" do %>
  <div data-controller="carousel" class="overflow-hidden w-full relative h-[500px]">
    <%= render partial: "image_movies/image_movies", locals: { imagens: @images } %>
    <%= render(ButtonComponent.new("Anterior", "#", icon: "chevron-left",
      class: "absolute left-4 top-1/2 -translate-y-1/2 bg-black bg-opacity-20 text-white px-4 py-2 rounded-full shadow-lg z-10 flex items-center justify-center",
      "data-action": "carousel#prev"
    )) %>

    <%= render(ButtonComponent.new("Próximo", "#", icon: "chevron-right",
      class: "absolute right-4 top-1/2 -translate-y-1/2 bg-black bg-opacity-80 text-white px-4 py-2 rounded-full shadow-lg z-10 flex items-center justify-center",
      "data-action": "carousel#next"
    )) %>
  </div>
<% end %>