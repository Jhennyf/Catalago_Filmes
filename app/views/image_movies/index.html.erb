<%= turbo_frame_tag "banner_images" do %>
  <div data-controller="carousel" class="relative w-full h-[500px] overflow-hidden">
    <%= render partial: "image_movies/image_movies", locals: { imagens: @images } %>
    <button data-action="carousel#prev" class="absolute left-4 top-1/2 -translate-y-1/2 z-10">Anterior</button>
    <button data-action="carousel#next" class="absolute right-4 top-1/2 -translate-y-1/2 z-10">Próximo</button>
  </div>
<% end %>